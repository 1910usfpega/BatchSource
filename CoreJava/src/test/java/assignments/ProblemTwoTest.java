package assignments;

import static org.junit.jupiter.api.Assertions.*;

import org.junit.Assert;
import org.junit.Before;
import org.junit.jupiter.api.Test;

public class ProblemTwoTest {
	
    @Test
    public void FibonacciTest() {//write different test cases and test for edge cases, normal cases something like below.
         //Assert.assertEquals(ProblemTwo.fSequence(1), 1);
    }
}
